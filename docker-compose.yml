version: '3.8'

services:
  backend:
    build: ./
    ports:
      - "8000:8000"
    environment:
      - MODEL_PATH=/app/models
    volumes:
      - ./models:/app/models

  frontend:
    build: ./client/
    ports:
      - "5173:5173"
    depends_on:
      - backend